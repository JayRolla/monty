#include "monty.h"

/**
 * nop_op - Do nothing
 * @stack_head: Pointer to the head of the stack (not used)
 * @line_num: Line where the instruction is found (not used)
 */
void nop_op(stack_t *stack_head, unsigned int line_num)
{
    (void)stack_head;
    (void)line_num;
}
